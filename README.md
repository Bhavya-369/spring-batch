# Invoice Generation with Spring Batch

This is a SpringBoot POC that uses Spring Batch for automated data processing. It reads the order data from the CSV files, applies tax and discount calculations, generates invoices, stores in mysql database. 
